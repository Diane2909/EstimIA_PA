# EstimIA — Dépendances de production pour l'image Docker de l'API
# Sous-ensemble volontairement restreint de requirements.txt : ni la pipeline
# de données (geopandas/shapely/pyarrow), ni les notebooks, ni le linting.
# Installer avec : pip install -r requirements-prod.txt

fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-dotenv==1.0.1
joblib==1.4.2
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
# Téléchargement du modèle ML au démarrage (backend/scripts/download_model.py) :
# boto3 pour AWS S3, requests pour Azure Blob Storage (SAS). Voir docs/aws-setup.md
# et docs/azure-setup.md — le script choisit automatiquement selon les env vars.
boto3==1.34.144
requests==2.31.0
